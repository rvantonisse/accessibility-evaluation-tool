import { Canvas, Controls, Meta } from '@storybook/blocks';

import * as FieldStories from './Field.stories.svelte';

<Meta title="Forms/Field/README" />

# Field

A Field groups a Label with a user Input.

A Field is used to have consistent layout options for a diverse set of user interfaces.

<Canvas of={FieldStories.Default} />
<Controls of={FieldStories.Default} />